class AppConstants {
  const AppConstants._();

  static const appName = 'GPS Faker';
  static const defaultIntervalMs = 1000;
  static const defaultAccuracyMeters = 5.0;
  static const minIntervalMs = 250;
  static const maxIntervalMs = 60000;
}
